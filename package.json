{
  "name": "wordpress-parser",
  "version": "0.1.0",
  "description": "Generate a reusable skill.md and optional RAG JSON from WordPress content.",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
